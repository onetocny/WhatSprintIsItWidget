This folder is referenced by the widget provider's AppExtension PublicFolder
attribute in Package.appxmanifest. The Windows Widgets host requires the folder
to exist in the package. Widget definitions are supplied inline in the manifest,
so no additional files are required here.
